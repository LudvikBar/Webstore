# Webstore
<br>

As this project is mainly for learning and will not be actully used, the auth information normaly stored in a .env file **is NOT in a .env file** and it is therefor viewable for everyone with access to the code.

<br>

### In this project I will create a webstore, with auth, and the normal webstore features.


The project will utilize fetch, POST, express + bcrypt and MySQL/MariaDB.

<br>
<br>
Setup guide will come
